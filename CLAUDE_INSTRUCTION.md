# 🤖 ИНСТРУКЦИЯ ДЛЯ CLAUDE

**Проект:** Telegram-бот продажи доступа к каналам  
**Версия:** 4.0  
**Дата создания:** Январь 2025

---

## 🔐 ДОСТУПЫ И ТОКЕНЫ

### GitHub Repository
```
URL: https://github.com/ironsan2kk-pixel/TGconst
Token: (хранится в Project Files или памяти Claude)
Branch: main
```

### Команды для работы с GitHub API
```bash
# Получить список файлов в папке
curl -s -H "Authorization: token YOUR_TOKEN" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/{path}"

# Создать/обновить файл
curl -X PUT -H "Authorization: token YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/{path}" \
  -d '{
    "message": "commit message",
    "content": "base64_encoded_content",
    "sha": "file_sha_if_updating"
  }'
```

---

## 📋 ФОРМАТ РАБОТЫ

### Этапы называются "Чат N"
- Пользователь пишет: **"Чат 1"** → Claude делает ВСЁ из Чат 1
- Пользователь проверяет → пишет **"ОК"** или замечания
- Пользователь пишет: **"Чат 2"** → Claude делает Чат 2
- И так далее до конца (6 чатов)

### После каждого Чата:
1. Claude создаёт все файлы локально
2. Claude выгружает файлы в GitHub репозиторий
3. Claude делает коммит с понятным сообщением
4. Claude обновляет CHECKLIST.md (отмечает выполненное)
5. Claude пишет что сделано и что проверить

---

## 🖥️ ПЛАТФОРМА: WINDOWS SERVER

### Важные правила для Windows:
1. **Кодировка .bat файлов:** UTF-8, использовать `chcp 65001` в начале
2. **Виртуальное окружение:** `venv\Scripts\activate` (не source)
3. **Русские символы:** обязательно `chcp 65001` для корректного отображения

### Шаблон .bat файла:
```batch
@echo off
chcp 65001 > nul
cd /d "%~dp0"

echo ========================================
echo   Название скрипта
echo ========================================
echo.

:: Команды здесь

pause
```

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА (v4.0)

### Ключевые изменения от v3.0:
- **Пакеты вместо тарифов** — Package + PackageOptions (несколько сроков/цен)
- **Оплата TON/TRC20** — прямая проверка транзакций по hash (без CryptoBot)
- **Контент-менеджер** — таблица `texts` для всех редактируемых текстов
- **Фиксированный шаблон бота** — без конструктора меню
- **Очередь задач** — таблица `tasks` для связи бот ↔ userbot

### Ключевые особенности:
- **Один бот** (не конструктор множества ботов)
- **Пакеты каналов** — один пакет = несколько каналов + варианты сроков
- **Два языка** — русский и английский
- **Пробный период** — 3/5/7 дней опционально
- **Промокоды** — скидка % или фикс. сумма
- **Тёмная тема** — в админке

---

## 🗄️ КЛЮЧЕВЫЕ ТАБЛИЦЫ БД

- `packages` — пакеты (название, описание)
- `package_channels` — связь пакет ↔ каналы
- `package_options` — варианты (30/90/365 дней, цена, trial)
- `channels` — каналы Telegram
- `wallets` — кошельки TON/TRC20
- `users` — пользователи (+ trial_used)
- `subscriptions` — подписки (status: active/expired/cancelled/trial)
- `payments` — платежи (network, tx_hash, status)
- `texts` — контент-менеджер (ключ → текст RU/EN)
- `faq_items` — FAQ вопросы-ответы
- `tasks` — очередь задач для userbot (invite/kick)
- `promocodes` — промокоды
- `broadcasts` — рассылки

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### НЕ ЗАБЫТЬ:
1. `.gitignore` — исключить: data/, venv/, .env, __pycache__/, node_modules/, logs/
2. `.env.example` — шаблон всех переменных окружения
3. Все .bat файлы с `chcp 65001` в начале
4. CHECKLIST.md обновлять после каждого чата

### ИЗБЕГАТЬ:
- Docker (проект без Docker)
- CryptoBot (используем прямую проверку транзакций)
- Конструктор меню (фиксированный шаблон)
- Хардкода токенов в коде

---

## 📊 ЭТАПЫ (6 ЧАТОВ)

| Чат | Название | Результат |
|-----|----------|-----------|
| 1 | Структура + БД | install.bat → БД создана, /health |
| 2 | Бот — ядро | /start → меню → пакеты |
| 3 | Оплата + Userbot | Полный цикл оплаты |
| 4 | Подписки + фичи | Промокоды, FAQ, trial |
| 5 | Backend API | Swagger полный |
| 6 | Frontend | Готовая админка |

---

**Эта инструкция — главный источник правды для Claude по данному проекту.**
