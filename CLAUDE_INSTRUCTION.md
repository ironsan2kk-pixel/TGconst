# 🤖 ИНСТРУКЦИЯ ДЛЯ CLAUDE

**Проект:** Telegram-бот продажи доступа к каналам  
**Версия:** 4.0  
**Дата:** Январь 2025

---

## 🔐 ДОСТУПЫ И ТОКЕНЫ

### GitHub Repository
```
URL: https://github.com/ironsan2kk-pixel/TGconst
Token: Указывается в начале каждого чата или в Project Instructions
Branch: main
```

### Команды для работы с GitHub API
```bash
# Получить список файлов в папке
curl -s -H "Authorization: token TOKEN" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/{path}"

# Создать/обновить файл
curl -X PUT -H "Authorization: token TOKEN" \
  -H "Content-Type: application/json" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/{path}" \
  -d '{
    "message": "commit message",
    "content": "base64_encoded_content",
    "sha": "file_sha_if_updating"
  }'

# Удалить файл
curl -X DELETE -H "Authorization: token TOKEN" \
  -H "Content-Type: application/json" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/{path}" \
  -d '{
    "message": "delete message",
    "sha": "file_sha"
  }'
```

---

## 📋 ФОРМАТ РАБОТЫ

### Этапы называются "Чат N" (всего 6 чатов)
- Пользователь пишет: **"Чат 1"** → Claude делает ВСЁ из Чат 1
- Пользователь проверяет → пишет **"ОК"** или замечания
- Пользователь пишет: **"Чат 2"** → Claude делает Чат 2
- И так далее до Чат 6

### После каждого Чата:
1. Claude создаёт все файлы локально
2. Claude выгружает файлы в GitHub репозиторий
3. Claude делает коммит с понятным сообщением
4. Claude обновляет CHECKLIST.md (отмечает выполненное)
5. Claude пишет что сделано и что проверить

### Формат коммитов:
```
Чат N: краткое описание

- Что сделано 1
- Что сделано 2
- Что сделано 3
```

---

## 🖥️ ПЛАТФОРМА: WINDOWS SERVER

### Важные правила для Windows:
1. **Кодировка .bat файлов:** UTF-8, использовать `chcp 65001` в начале
2. **Переносы строк:** использовать CRLF для .bat файлов
3. **Пути:** использовать обратный слеш или прямой (Python понимает оба)
4. **Виртуальное окружение:** `venv\Scripts\activate` (не source)
5. **Русские символы:** обязательно `chcp 65001` для корректного отображения

### Шаблон .bat файла:
```batch
@echo off
chcp 65001 > nul
cd /d "%~dp0"

echo ========================================
echo   Название скрипта
echo ========================================
echo.

:: Команды здесь

pause
```

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

### Ключевые особенности:
- **Один бот** с фиксированным шаблоном (не конструктор)
- **Пакеты каналов** — один пакет = несколько каналов + варианты сроков (30/90/365 дней)
- **Два языка** — русский и английский
- **Пробный период** — 3/5/7 дней опционально
- **Оплата криптой** — TON и TRC20 (USDT), проверка по hash
- **Контент-менеджер** — все тексты в БД, редактируются в админке
- **Тёмная тема** — в админке (переключатель)
- **Уведомления** — о продлении подписки, админам о покупках

### НЕ используем:
- ❌ Конструктор меню (фиксированная структура)
- ❌ CryptoBot (прямая проверка транзакций)
- ❌ Docker

### Структура папок:
```
telegram-channel-bot/
├── bot/                    # Telegram бот (Aiogram 3)
│   ├── handlers/           
│   ├── keyboards/          
│   ├── callbacks/          
│   ├── middlewares/        
│   ├── services/           
│   ├── locales/            # Дефолтные тексты
│   └── models/             
├── userbot/                # Pyrogram userbot
│   └── actions/            
├── admin/                  # Backend API (FastAPI)
│   ├── api/                
│   └── schemas/            
├── frontend/               # React админка
│   └── src/
├── data/                   # База данных и бэкапы
├── scripts/                # Утилиты Python
└── *.bat                   # Windows скрипты
```

---

## 📝 ПРАВИЛА НАПИСАНИЯ КОДА

### Python:
- Python 3.11+
- Type hints везде
- Async/await для I/O операций
- Pydantic для валидации
- SQLAlchemy 2.0 с async
- Docstrings для функций и классов

### React:
- React 18+ с hooks
- Tailwind CSS для стилей
- Тёмная тема через CSS переменные и localStorage

---

## 🗄️ КЛЮЧЕВЫЕ ТАБЛИЦЫ БД

### Новые таблицы (vs старая версия):
- `packages` — пакеты (вместо tariffs)
- `package_options` — варианты подписки (30/90/365 дней)
- `package_channels` — связь пакет ↔ каналы
- `texts` — контент-менеджер (все тексты бота)
- `tasks` — очередь для userbot (invite/kick)

### Убрали:
- `tariffs`, `tariff_channels` → заменены на packages
- `menu_items` → фиксированное меню в коде

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### НЕ ЗАБЫТЬ:
1. `.gitignore` — исключить: data/, venv/, .env, __pycache__/, node_modules/, logs/
2. `.env.example` — шаблон всех переменных окружения
3. Все .bat файлы с `chcp 65001` в начале
4. CHECKLIST.md обновлять после каждого чата
5. Дефолтные тексты загружать при первом запуске setup_db.py

### ИЗБЕГАТЬ:
- Docker (проект без Docker)
- Сложных зависимостей
- Хардкода токенов в коде
- Синхронных операций где можно async
- Конструктора меню (фиксированный шаблон)

---

## 🔄 WORKFLOW РАЗРАБОТКИ

### При получении команды "Чат N":

1. **Прочитать** CHECKLIST.md — что нужно сделать
2. **Создать** все файлы локально
3. **Проверить** синтаксис и импорты
4. **Выгрузить** в GitHub через API
5. **Обновить** CHECKLIST.md — отметить выполненное
6. **Отчитаться** — что сделано, как проверить

---

## 🎯 ГЛАВНЫЕ ПРИНЦИПЫ

1. **Фиксированный шаблон** — меню и структура бота в коде, тексты в БД
2. **Windows Server** — .bat файлы с правильной кодировкой
3. **Чистый код** — type hints, async, документация
4. **Работающий продукт** — каждый чат = рабочий результат
5. **6 чатов** — от структуры до готового frontend

---

**Эта инструкция — главный источник правды для Claude по данному проекту.**
