# 📋 ИНСТРУКЦИЯ ДЛЯ CLAUDE: Проект TGconst

**Версия:** 4.0  
**Дата создания:** Январь 2025  
**Тип проекта:** Telegram-бот продажи доступа к каналам

---

## 🔐 ДОСТУПЫ И ТОКЕНЫ

### GitHub Repository
- **URL:** https://github.com/ironsan2kk-pixel/TGconst
- **Token:** Запрашивать у пользователя в начале каждого чата!

### Работа с GitHub API
```bash
# Получить содержимое файла
curl -s -H "Authorization: token TOKEN" \
  "https://raw.githubusercontent.com/ironsan2kk-pixel/TGconst/main/FILENAME"

# Получить список файлов в папке
curl -s -H "Authorization: token TOKEN" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/PATH"

# Создать/обновить файл
curl -X PUT -H "Authorization: token TOKEN" \
  -H "Content-Type: application/json" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/FILENAME" \
  -d '{
    "message": "Commit message",
    "content": "BASE64_ENCODED_CONTENT",
    "sha": "SHA_IF_UPDATING"
  }'
```

---

## 📝 ФОРМАТ РАБОТЫ

### Терминология
- Этапы называются **"Чат 1"**, **"Чат 2"**, и т.д. (НЕ "Этап")
- Всего **8 чатов**

### Процесс работы
1. Пользователь пишет **"Чат N"**
2. Claude выполняет ВСЁ из чек-листа для этого чата
3. Claude выгружает файлы в GitHub репозиторий
4. Claude сообщает что сделано и как проверить
5. Пользователь проверяет
6. Пользователь пишет **"Чат N+1"** или замечания

### Коммиты
- Формат: `Чат N: краткое описание`
- Примеры:
  - `Чат 1: Структура проекта и модели БД`
  - `Чат 2: Ядро бота, handlers, локализация`

---

## 🖥️ ПЛАТФОРМА: WINDOWS

### Важно помнить
- **Все .bat файлы** должны начинаться с:
```batch
@echo off
chcp 65001 > nul
```

### Кодировка файлов
- Python файлы: UTF-8 (без BOM)
- .bat файлы: UTF-8 + chcp 65001
- .md файлы: UTF-8

---

## 🗂️ СТРУКТУРА ПРОЕКТА

```
telegram-channel-bot/
├── .env.example
├── .gitignore
├── requirements.txt
├── README.md
├── MASTER_PLAN.md
├── CHECKLIST.md
├── CLAUDE_INSTRUCTION.md
├── data/                 # SQLite база (в .gitignore)
├── bot/                  # Telegram бот (Aiogram)
├── userbot/              # Pyrogram userbot
├── admin/                # FastAPI backend
├── frontend/             # React админка
└── scripts/              # .bat скрипты
```

---

## 📊 ЧАТЫ РАЗРАБОТКИ

| # | Название | Основные задачи |
|---|----------|-----------------|
| 1 | Структура и БД | .gitignore, requirements.txt, модели SQLAlchemy |
| 2 | Бот — Ядро | Aiogram, handlers, middleware, локализация RU/EN |
| 3 | CryptoBot | API клиент, инвойсы, webhook |
| 4 | Userbot | Pyrogram, invite/kick |
| 5 | Подписки | Проверка, напоминания, автокик |
| 6 | Фичи бота | Промокоды, /admin, ручное подтверждение |
| 7 | Рассылки | Создание, фильтры, отправка |
| 8 | Админка | FastAPI + React с темой и графиками |

---

## 🚨 ОСОБЫЕ ТРЕБОВАНИЯ

1. **Один бот** (НЕ конструктор) — одна база data/bot.db
2. **Пакеты каналов** — тариф → несколько каналов (tariff_channels)
3. **Два языка** — RU/EN через bot/locales/
4. **Deep Links** — /start tariff_N, utm_X, promo_CODE
5. **Напоминания** — за N дней со скидкой
6. **Ручное подтверждение** — status="manual", confirmed_by
7. **Тёмная тема** — ThemeContext в React
8. **Графики** — Recharts на Dashboard

---

## ✅ КОНТРОЛЬНЫЕ ТОЧКИ

### После каждого чата:
1. Все файлы созданы
2. Код синтаксически корректен
3. .bat файлы с UTF-8 + chcp 65001
4. Коммит в репо
5. Обновить CHECKLIST.md (✅)

---

## 📞 ФОРМАТ ОТВЕТОВ

### При начале чата:
```
## 🚀 Чат N: Название

### Создаю файлы:
1. file1.py
2. file2.py

### Выгружаю в репо...

### ✅ Чат N завершён!

**Создано:** X файлов
**Коммит:** Чат N: описание

### Как проверить:
...

### Следующий шаг:
Напиши "Чат N+1"
```

---

**Эта инструкция — главный источник правды!**
