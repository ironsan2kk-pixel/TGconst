# 📋 ИНСТРУКЦИЯ ДЛЯ CLAUDE: Проект TGconst

## 🔑 ДАННЫЕ ДОСТУПА

```
Репозиторий: https://github.com/ironsan2kk-pixel/TGconst
API: https://api.github.com/repos/ironsan2kk-pixel/TGconst
Токен: ЗАПРОСИТЬ У ПОЛЬЗОВАТЕЛЯ В НАЧАЛЕ ЧАТА
```

---

## 🚀 ПОРЯДОК РАБОТЫ В КАЖДОМ НОВОМ ЧАТЕ

### ШАГ 0: Запросить токен у пользователя

Если токен не был предоставлен — попросить:
"Скинь GitHub токен для доступа к репозиторию"

### ШАГ 1: Проверить текущее состояние репозитория

```bash
# Получить список всех файлов в репо
curl -s -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/

# Прочитать CHECKLIST.md для определения текущего прогресса
curl -s -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/CHECKLIST.md | \
  python3 -c "import sys,json,base64; d=json.load(sys.stdin); print(base64.b64decode(d['content']).decode())"
```

### ШАГ 2: Определить какой этап делаем

- Посмотреть CHECKLIST.md — какие этапы уже ✅
- Спросить пользователя какой этап делать
- **НЕ НАЧИНАТЬ пока пользователь не скажет "поехали"**

### ШАГ 3: Выполнить этап

- Создать все файлы локально в /home/claude/TGconst/
- Проверить синтаксис (python -m py_compile)
- Запушить каждый файл в GitHub

### ШАГ 4: Пуш файлов в GitHub

```bash
# Создать/обновить файл
CONTENT=$(cat /path/to/file | base64 -w 0)

# Для НОВОГО файла:
curl -s -X PUT \
  -H "Authorization: token $TOKEN" \
  -H "Content-Type: application/json" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/path/to/file \
  -d "{\"message\": \"Commit message\", \"content\": \"$CONTENT\"}"

# Для ОБНОВЛЕНИЯ файла (нужен SHA):
SHA=$(curl -s -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/path/to/file | \
  grep -o '"sha": "[^"]*"' | head -1 | cut -d'"' -f4)

curl -s -X PUT \
  -H "Authorization: token $TOKEN" \
  -H "Content-Type: application/json" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/path/to/file \
  -d "{\"message\": \"Commit message\", \"sha\": \"$SHA\", \"content\": \"$CONTENT\"}"
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
TGconst/
├── requirements.txt          # Python зависимости
├── .env.example              # Пример переменных окружения
├── README.md                 # Документация
├── MASTER_PLAN.md            # Мастер-план
├── CHECKLIST.md              # Чек-лист
│
├── data/                     # Данные (SQLite базы)
│   ├── main.db               # Главная БД (админы, боты)
│   └── bots/{uuid}/bot.db    # БД каждого бота
│
├── backend/
│   ├── run.py                # Точка входа FastAPI
│   ├── app/
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── database.py
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── api/
│   │   ├── services/
│   │   └── utils/
│   │
│   └── bot_template/         # Шаблон Telegram бота
│
├── userbot/                  # Pyrogram userbot
│
├── frontend/                 # React админка
│
└── scripts/                  # Скрипты деплоя
    └── supervisor/
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **БЕЗ DOCKER** — чистый Python + venv + supervisor
2. **SQLite для каждого бота** — data/bots/{uuid}/bot.db
3. **Главная база** — data/main.db (админы, список ботов)
4. **Не начинать этап** пока пользователь не скажет "поехали"
5. **Каждый файл** — отдельный коммит с понятным сообщением
6. **Проверять синтаксис** перед пушем

---

## 📊 ЭТАПЫ

| # | Этап | Статус |
|---|------|--------|
| 1 | Структура проекта | ✅ |
| 2 | База данных — Модели | ⬜ |
| 3 | Auth API | ⬜ |
| 4 | CRUD ботов | ⬜ |
| 5 | Каналы и тарифы | ⬜ |
| 6 | Промокоды | ⬜ |
| 7 | Рассылки | ⬜ |
| 8 | Шаблон бота | ⬜ |
| 9 | CryptoBot оплата | ⬜ |
| 10 | Userbot | ⬜ |
| 11 | Автокик | ⬜ |
| 12 | Бот: промокоды | ⬜ |
| 13 | Оркестратор | ⬜ |
| 14 | Frontend | ⬜ |
| 15 | Деплой | ⬜ |

---

**Эту инструкцию читать в начале КАЖДОГО нового чата!**
