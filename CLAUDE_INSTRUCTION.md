# 🤖 ИНСТРУКЦИЯ ДЛЯ CLAUDE

**Проект:** Telegram-бот продажи доступа к каналам  
**Репозиторий:** https://github.com/ironsan2kk-pixel/TGconst  
**Версия:** 3.0

---

## 🔑 ДОСТУПЫ

### GitHub:
```
Репозиторий: https://github.com/ironsan2kk-pixel/TGconst
Токен: [ПЕРЕДАЁТСЯ В НАЧАЛЕ КАЖДОГО ЧАТА]
API URL: https://api.github.com/repos/ironsan2kk-pixel/TGconst
```

> **ВАЖНО:** Токен GitHub передаётся пользователем в начале каждого нового чата.
> Claude запоминает его и использует для всех операций с репозиторием.

### Команды GitHub API:

**Список файлов:**
```bash
curl -s -H "Authorization: token $TOKEN" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/"
```

**Содержимое файла:**
```bash
curl -s -H "Authorization: token $TOKEN" \
  "https://raw.githubusercontent.com/ironsan2kk-pixel/TGconst/main/PATH"
```

**Создать/обновить файл:**
```bash
SHA=$(curl -s -H "Authorization: token $TOKEN" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/PATH" \
  | python3 -c "import sys,json; print(json.load(sys.stdin).get('sha',''))")

curl -X PUT -H "Authorization: token $TOKEN" \
  "https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/PATH" \
  -d '{"message": "msg", "content": "BASE64", "sha": "SHA"}'
```

---

## 📋 ФОРМАТ РАБОТЫ

### Этапы называются "Чат N":
- Чат 1, Чат 2, ... Чат 8

### Процесс:
1. Пользователь пишет **"Чат N"**
2. Claude делает ВСЁ из чеклиста
3. Claude пушит все файлы в GitHub
4. Claude отчитывается что сделано
5. Пользователь проверяет
6. Пользователь пишет **"ОК"** или замечания
7. Переход к следующему чату

### Коммиты:
```
Чат 1: структура проекта и модели БД
Чат 2: ядро бота с локализацией
Чат 3: интеграция CryptoBot
...
```

---

## 🖥️ ПЛАТФОРМА: Windows Server

### Все .bat файлы:
- Кодировка UTF-8
- Начинать с `chcp 65001 >nul`
- Пути через `%~dp0`

### Шаблон .bat:
```batch
@echo off
chcp 65001 >nul
cd /d "%~dp0.."

echo ========================================
echo   Название скрипта
echo ========================================

if not exist "venv\Scripts\activate.bat" (
    echo [ОШИБКА] venv не найден!
    pause
    exit /b 1
)

call venv\Scripts\activate.bat
python script.py

pause
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
telegram-channel-bot/
├── .env.example
├── .gitignore
├── requirements.txt
├── README.md
├── MASTER_PLAN.md
├── CHECKLIST.md
├── CLAUDE_INSTRUCTION.md
│
├── data/                    # В .gitignore
├── bot/                     # Telegram бот (Aiogram 3)
├── userbot/                 # Pyrogram userbot
├── admin/                   # FastAPI backend
├── frontend/                # React + Tailwind
└── scripts/                 # Windows .bat
```

---

## 🗄️ КЛЮЧЕВЫЕ ОСОБЕННОСТИ

### 1. Один бот (не конструктор):
- Одна БД: `data/bot.db`
- Нет таблицы bots
- Сразу к делу

### 2. Пакеты каналов:
- Tariff → TariffChannel → Channel
- Один тариф = несколько каналов

### 3. Два языка (RU/EN):
- `bot/locales/ru.py`, `bot/locales/en.py`
- Функция `t(key, lang, **kwargs)`
- Поля: name_ru, name_en, description_ru, description_en

### 4. Deep Links:
```
?start=tariff_5     → к тарифу
?start=ref_google   → источник
?start=promo_SAVE   → промокод
```

### 5. Напоминания:
- За 3 дня и 1 день до истечения
- Со скидкой на продление
- Флаги reminded_3d, reminded_1d

### 6. Ручное подтверждение:
- Payment.status = 'manual'
- Payment.confirmed_by = admin_tg_id
- В боте и веб-панели

### 7. Тёмная тема:
- ThemeContext в React
- Tailwind dark: классы
- localStorage

### 8. Графики:
- Recharts
- analytics_daily таблица
- /analytics/* endpoints

---

## ⚠️ ПРАВИЛА

### Кодировка:
- Все файлы UTF-8
- .bat с `chcp 65001`

### Python:
- Type hints везде
- SQLAlchemy 2.0 (Mapped)
- Async везде

### Безопасность:
- Нет токенов в коде
- bcrypt для паролей
- JWT авторизация

---

## 📝 ЧЕКЛИСТ ПЕРЕД ПУШЕМ

- [ ] Файлы созданы по чеклисту
- [ ] Синтаксис корректный
- [ ] Импорты правильные
- [ ] .bat с UTF-8
- [ ] Нет хардкода токенов
- [ ] Пути относительные

---

## 🔄 ТЕКУЩЕЕ СОСТОЯНИЕ

### Выполнено:
- [x] MASTER_PLAN.md v3.0
- [x] CHECKLIST.md v3.0
- [x] CLAUDE_INSTRUCTION.md

### Следующий: Чат 1

### Прогресс:
| Чат | Статус |
|-----|--------|
| 1 | ⬜ |
| 2 | ⬜ |
| 3 | ⬜ |
| 4 | ⬜ |
| 5 | ⬜ |
| 6 | ⬜ |
| 7 | ⬜ |
| 8 | ⬜ |

---

## 📞 ОТЧЁТНОСТЬ

### При начале:
```
Начинаю Чат N:
- Задача 1
- Задача 2
...
```

### При завершении:
```
✅ Чат N завершён!

Создано: X файлов
Изменено: Y файлов
Коммит: "Чат N: описание"

Проверь и напиши "ОК" или замечания.
```

---

**Claude готов. Жду "Чат 1"!**
