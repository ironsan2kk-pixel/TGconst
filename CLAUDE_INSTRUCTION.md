# 📋 ИНСТРУКЦИЯ ДЛЯ CLAUDE: Проект TGconst

## 🔑 ДАННЫЕ ДОСТУПА

```
Репозиторий: https://github.com/ironsan2kk-pixel/TGconst
API: https://api.github.com/repos/ironsan2kk-pixel/TGconst
Токен: ЗАПРОСИТЬ У ПОЛЬЗОВАТЕЛЯ В НАЧАЛЕ ЧАТА
```

---

## 🚀 ПОРЯДОК РАБОТЫ В КАЖДОМ НОВОМ ЧАТЕ

### ШАГ 0: Запросить токен у пользователя

Если токен не был предоставлен — попросить:
"Скинь GitHub токен для доступа к репозиторию TGconst"

### ШАГ 1: Проверить текущее состояние репозитория

```bash
# Получить список файлов
curl -s -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/

# Прочитать CHECKLIST.md
curl -s -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/CHECKLIST.md | \
  python3 -c "import sys,json,base64; d=json.load(sys.stdin); print(base64.b64decode(d['content']).decode())"
```

### ШАГ 2: Определить какой этап делаем

- Посмотреть CHECKLIST.md — какие этапы уже ✅
- Спросить пользователя какой этап делать
- **НЕ НАЧИНАТЬ пока пользователь не скажет "поехали"**

---

## ⚡ ПЛАН ВЫПОЛНЕНИЯ ЭТАПА

```
1️⃣ Создать все файлы этапа локально
2️⃣ Проверить синтаксис (python -m py_compile)
3️⃣ ВЫГРУЗИТЬ ВСЁ В РЕПОЗИТОРИЙ
4️⃣ Написать пользователю: "проверим"
5️⃣ ЖДАТЬ пока пользователь напишет "проверка"
6️⃣ Проверить работоспособность (запустить, протестировать)
7️⃣ Обновить CHECKLIST.md в репо (отметить ✅)
8️⃣ Сообщить результат
```

**ВАЖНО:** 
- Сначала ВСЕГДА выгружаем в репо
- Потом пишем "проверим" и ЖДЁМ
- Только после "проверка" от пользователя — проверяем

---

## 📤 КАК ПУШИТЬ В GITHUB

```bash
# Для НОВОГО файла:
CONTENT=$(cat /path/to/file | base64 -w 0)
curl -s -X PUT \
  -H "Authorization: token $TOKEN" \
  -H "Content-Type: application/json" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/path/to/file \
  -d "{\"message\": \"Commit message\", \"content\": \"$CONTENT\"}"

# Для ОБНОВЛЕНИЯ файла:
SHA=$(curl -s -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/path/to/file | \
  grep -o '"sha": "[^"]*"' | head -1 | cut -d'"' -f4)

curl -s -X PUT \
  -H "Authorization: token $TOKEN" \
  -H "Content-Type: application/json" \
  https://api.github.com/repos/ironsan2kk-pixel/TGconst/contents/path/to/file \
  -d "{\"message\": \"Commit message\", \"sha\": \"$SHA\", \"content\": \"$CONTENT\"}"
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
TGconst/
├── requirements.txt          # Python зависимости
├── .env.example              # Пример переменных окружения
├── MASTER_PLAN.md            # Мастер-план
├── CHECKLIST.md              # Чек-лист (обновлять после каждого этапа!)
├── CLAUDE_INSTRUCTION.md     # Эта инструкция
│
├── data/                     # SQLite базы
│   ├── main.db               # Главная БД
│   └── bots/{uuid}/bot.db    # БД каждого бота
│
├── backend/
│   ├── run.py
│   ├── app/
│   └── bot_template/
│
├── userbot/
├── frontend/
└── scripts/
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **БЕЗ DOCKER** — чистый Python + venv + supervisor
2. **SQLite для каждого бота** — data/bots/{uuid}/bot.db
3. **Главная база** — data/main.db
4. **Не начинать** пока пользователь не скажет "поехали"
5. **Сначала пуш** → потом "проверим" → ждать "проверка"
6. **Обновлять CHECKLIST.md** после каждого этапа

---

## 📊 ЭТАПЫ

| # | Этап | Статус |
|---|------|--------|
| 1 | Структура проекта | ✅ |
| 2 | База данных — Модели | ⬜ |
| 3 | Auth API | ⬜ |
| 4 | CRUD ботов | ⬜ |
| 5 | Каналы и тарифы | ⬜ |
| 6 | Промокоды | ⬜ |
| 7 | Рассылки | ⬜ |
| 8 | Шаблон бота | ⬜ |
| 9 | CryptoBot оплата | ⬜ |
| 10 | Userbot | ⬜ |
| 11 | Автокик | ⬜ |
| 12 | Бот: промокоды | ⬜ |
| 13 | Оркестратор | ⬜ |
| 14 | Frontend | ⬜ |
| 15 | Деплой | ⬜ |

---

**Эту инструкцию читать в начале КАЖДОГО нового чата!**
